// import { doc, setDoc } from "firebase/firestore";
// import { db } from "../firestore";

// export async function submitProviderApplication(uid, data) {
//   await setDoc(doc(db, "providerApplications", uid), {
//     ...data,
//     status: "pending",
//     createdAt: new Date(),
//   });
// }
